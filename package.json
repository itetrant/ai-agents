{
  "name": "ai-agents",
  "version": "0.1.0",
  "description": "A small team of specialist AI agents (orchestrator, backend, frontend, database, devops, qa, reviewer) that collaborate over the Claude API to build full-stack applications.",
  "type": "module",
  "bin": {
    "ai-agents": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "commander": "^12.1.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
